---
import '../styles/about.css';
import { Image } from 'astro:assets';
import aboutImage from '../assets/images/about-brewery.jpg';

export interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<section id="about" class={`about-section ${className}`} aria-labelledby="about-title">
  <div class="about-container">

    <!-- En-tête centré -->
    <div class="about-header reveal">
      <h2 id="about-title" class="about-title">La Brasserie</h2>
      <p class="lead">
        Femto-brasserie en Nouvelle-Aquitaine. Des bières en petits lots, une approche empirique.
      </p>
    </div>

    <!-- Split : texte + image -->
    <div class="about-split">
      <div class="about-text reveal">
        <blockquote class="about-quote">
          &laquo;&nbsp;J'aime créer des bières qui racontent une histoire.&nbsp;&raquo;
        </blockquote>

        <p class="about-body">
          Je peux brasser une bière en deux batch de 30L.
          L'approche est artisanale mais les outils sont modernes
          ; chaque batch est tracé du grain à la bouteille.
        </p>

        <div class="about-stats">
          <span>&lt; 100L par brassin</span>
          <span>Petits lots</span>
          <span>Nouvelle-Aquitaine</span>
        </div>
      </div>

      <div class="about-image reveal">
        <Image
          src={aboutImage}
          alt="Intérieur de notre brasserie artisanale"
          loading="lazy"
          width={960}
          height={1200}
          quality={82}
          format="webp"
        />
      </div>
    </div>

  </div>
</section>
